/*
Assignment 3 - Theme Selector
Use ternary operator:
  - If isDarkMode is true, return Dark Theme
  - Otherwise return Light Theme
*/

void main() {
  bool isDarkMode = true;
  String mode = isDarkMode ? "Dark Mode" : "Light Mode";
  print(mode);
}